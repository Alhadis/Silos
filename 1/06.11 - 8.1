string[] nazwiskaAutorów = {"Leśmian", "Tuwim", "Miłosz", "Asnyk", "Mickiewicz", "Kochanowski", "Rej" };
            var nowaTablica = new string[3];
            //int dlugoscTablicy = nazwiskaAutorów.LongCount;
            //Console.WriteLine(dlugoscTablicy);
            //Console.WriteLine(nazwiskaAutorów[7], nazwiskaAutorów[1], nazwiskaAutorów[5]);
            Array.Sort(nazwiskaAutorów);
            foreach(string autor in nazwiskaAutorów)
            {
                Console.Write(autor + ", ");
            }
            Console.WriteLine(" ");
            Array.Reverse(nazwiskaAutorów);
            foreach (string autor in nazwiskaAutorów)
            {
                Console.Write(autor + ", ");
            }
            Array.Copy(nazwiskaAutorów, 0, nowaTablica, 3);
            foreach (string autor in nowaTablica)
            {
                Console.Write(autor + ", ");
            }


9.1
{
            List<string> autorzy = new List<string>();
            autorzy.Add("Mickiewicz");
            autorzy.Add("Słowacki");
            autorzy.Add("Brzechwa");
            autorzy.Add("Konopnicka");
            foreach (var nazwisko in autorzy)
            {
                Console.Write(nazwisko + ", ");
            }
            Console.WriteLine("\nIlość elementów: {0}", autorzy.Capacity);
            Console.WriteLine("Wybierz co chcesz zrobić. \n1-Sprawdź czy dane nazwisko jest na liście \n2-Dodaj dwa nazwiska do istniejącej listy. \n3-Usuń nazwisko z listy. \n4-Wyczyszczenie i wyświetlenie elementów listy.");
            var wybor = int.Parse(Console.ReadLine());
            switch (wybor)
            {
                case 1:

                    break;
            }
            
        }
        
        
        DICTIONARY - do dokonczenia case 3
        
        {
            Dictionary<string, string> słownik = new Dictionary<string, string>();
            słownik.Add("ks.1", "Harry Potter - J.K.Rowling");
            słownik.Add("ks.2", "Władca Pierścieni - Tolkien");
            słownik.Add("ks.3", "Web Shows - Krzysztof Gonciarz");
            słownik.Add("ks.4", "Superfood- Jaimie Oliver");
            foreach (var ksiazki in słownik)
            {
                Console.WriteLine(ksiazki);
            }
            Console.WriteLine("Wybierz opcję: \n1 - Dodaj książkę według klucza \n2 - Dodaj książkę według tytułu \n3 - Usuń nazwę książki (jeżeli takowa została podana)");
            var wybór = int.Parse(Console.ReadLine());
            switch (wybór)
            {
                case 1:
                    Console.WriteLine("Podaj klucz książki, który ma zostać sprawdzony");
                    string klucz1 = Console.ReadLine();
                    if (!słownik.ContainsKey(klucz1))
                    {
                        Console.WriteLine("Podaj tytuł, który ma zostać dodany do słownika:");
                        string tytul1 = Console.ReadLine();
                        słownik.Add(klucz1, tytul1);
                        Console.WriteLine("Pozycja została dodana");
                    }
                    foreach (KeyValuePair<string, string>x in słownik)
                    {
                        Console.WriteLine(x.Key + " " + x.Value);
                    }
                    break;
                case 2:
                    Console.WriteLine("Podaj tytuł, który ma zostać sprawdzony:");
                    string tytul2 = Console.ReadLine();
                    if (!słownik.ContainsValue(tytul2))
                    {
                        Console.WriteLine("Podaj klucz do książki");
                        string klucz2 = Console.ReadLine();
                        słownik.Add(klucz2, tytul2);
                        Console.WriteLine("Pozycja została dodana");
                    }
                    foreach (KeyValuePair<string, string>x in słownik)
                    {
                        Console.WriteLine(x.Key + " " + x.Value);
                    }
                    break;
                case 3:
                    Console.WriteLine("Podaj klucz do ksiązki, który ma zostać sprawdzony:");
                    string klucz3 = Console.ReadLine();

                    break;
            }


zadania na strinhch

{
            string napis1 = "ala ma kota";
            string napis2 = "ALA MA KOTA";
            int porownanie = String.Compare(napis1, napis2);
            Console.WriteLine(porownanie);
            string tekstSklejony = string.Concat(napis1, napis2);
            Console.WriteLine(tekstSklejony);
            string miejsce3 = napis1.Substring(2, 1);
            Console.WriteLine(miejsce3);
            Console.WriteLine(napis1.Length);
            int ma = napis1.IndexOf("ma");
            Console.WriteLine(ma);
            string wstaw = napis1.Insert(6, "psa i");
            Console.WriteLine(wstaw);
        }
