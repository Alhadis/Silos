Build a function that returns:
The mean of entries when it's raining
The mean of entries when it's not raining

import numpy as np

import pandas

def means(turnstile_weather):
    #total_days = turnstile_weather['rain'].count()
    total_rainy_days = len(turnstile_weather[turnstile_weather['rain']==1])
    total_non_rainy_days = len(turnstile_weather[turnstile_weather['rain']==0])
    
    turnstile_weather_rain = turnstile_weather[turnstile_weather['rain']==1]
    with_rain_mean = turnstile_weather_rain['ENTRIESn_hourly'].mean()
    
    turnstile_weather_not_rain = turnstile_weather[turnstile_weather['rain']==0]
    without_rain_mean = turnstile_weather_not_rain['ENTRIESn_hourly'].mean()
        
    #print(with_rain_mean)
    #print(without_rain_mean)
    
    
    return with_rain_mean, without_rain_mean, p # leave this line for the grader

means(turnstile_weather)
