void main()
{
 object oPC = GetEnteringObject();
 SendMessageToPC(oPC,"You find yourself on a rock ledge.  You will need to use ROCKS and ropes to descent the ledge, some 40 feet below.  You can then access the valley beyond.");
}
