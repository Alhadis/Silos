# Explicit return types
fn returns_unit : unit { }
fn returns_ascii : ascii { }

# Inferred Types
fn also_returns_unit { }
fn returns_int { return 1 + 1 }
fn returns_utf { return "the quick brown fox" }
