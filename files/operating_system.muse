#title Os
* Main functions
1. providing abstraction to user programs
2. managing the computer's resources

* Process
A process is basically a program in execution.Associated with each process is its address space.

In mqny operating systems, all the information about each process, other than the contents of its own address space, is stored in aan operating system table called process table.
