比较运算符：
	<  >	<=	>=	!= 	==
说明：比较运算符返回的是布尔类型的值
(都是相同优先级，从左向右进行运算)

数据对象的构造函数：
	float(obj) 用字符串或数字转换为浮点数，如果
	不给出参数，则返回0
	complex(r=0.0,i=0.0)用数字创建一个复数（实部为r,虚部为i）
	bool(x)  用x创建一个布尔值（True/Flase）

函数调用：
	函数名（传参列表）
	说明：函数调用是表达式，一定会返回一个对象的引用关系（或者返回None）

bool(x)返回假值的情况；
	None 空值
	False 布尔假值
	0     0.0   0j    所有的数字0
	————————以下内容后面讲——————————
	''   空字符串(中间不能有空格)
	()	 空元祖
	[]    空列表
	{}     空字典
	....
整数运算无误差，小数基本都会有精度丢失
pi=3.14
print(pi)
i=int(3.14)
print(i)
f=pi-i
print(f)
print(type(f))
print(str(f))
print(len(str(f)))
# print(double(pi-i))
a=bool(f)
print(a)

结果
3.14
3
0.14000000000000012
<class 'float'>
0.14000000000000012
19
True

（说明，float无len 函数，无double类型）

预置（内建）的数值型函数
abs(x)   	区x的绝对值
round(number[,ndigits])	对数值进行‘四舍五入’，ndigits是小数向右取整的位数，负数
表示向左取整
>>> round(12345.5)
12346
>>> round(1234.5)
1234

>>> x=123.135
>>> round(x,2)
123.14
>>> round(x,3)
123.135
>>> round(x,0)
123.0
>>> round(x,-1)
120.0
>>> round(x,-2)
100.0
>>> round(x,-3)
0.0

pow(x,y,z=None)		相当于x**y 或 x**y%z

help()查看函数帮助
>>>help（函数名）

语句:语句是python 执行的最小单位
语句由一些表达式组成，通常一条语句可以独立执行来
完成一本分事情并形成结果
（一条语句建议写在一行内）
多条语句写在一行内用分号（；）隔开
示例；
	print('dfs')
	x=100+200

	print('dfs');x=100+200

语句的而显示换行；
示例：
statement_newline.py
拆行符 \  （读作，反斜杠）
放在一行的末尾，表示下一行也是该行的语句

语句的隐式换行：
所有的括号内容换行称为隐式换行
括号：() [] {}

基本输入输出函数：
input:作用，从标准输入设备上（默认为键盘）读取一个字符串
（末尾的换行字符会被删除）
格式：
input('提示字符串')
说明：
返回输入的字符串

基本输出函数print
	作用：
	将一系列的值以字符形式输出到标准输出设置上（默认是终端）
	格式：
	print(value,...,sep='',end='\n',file=sys.stdout,flush=False)
	file:文件流对象，默认为（sys.stdout）
	flush：是否立即进行输出

练习：
	输入两个整数，分别用变量x,y来绑定
	a.计算这两个数的和，并打印结果
	b.计算这两个数的积，并打印结果
	c.计算x的y次并打印结果
2.分三次输入当前的小时，分钟，秒数
在终端打印出距离， 0:0：0过了多少秒。
条件表达式：
语法；
表达式1 if 真值表达式 else 表达式2
作用：更具真值表达式的取值（True/False）来决定执行表达式1还是表达式2并返回结果
x=y-10 if y>100 else y

pass语句
	作用：
	通常用来填充语法空白
	pass  又名空语句
	语法：
		pass

布尔运算：
	运算符：
		not  and  or
布尔非操作 not
	语法：
		not x
	作用：对x进行布尔取非

	运算符都是放在表达式里的

布尔或操作 or
语法；x or y来绑定：
作用：
	优先返回真值对象，如果x为True时返回x，否则返回y

and 优先返回假值对象。

>>> 1 and 0
0
>>> 0 and 1
0
>>> 1 and 1
1
>>> 0 and 0.0
0
>>> 0 or 0
0
>>> 0.0 or 0
0
>>> 1 or 0
1
>>> 0 or 1
1
>>> 1 or 1
1
>>>
正负号运算：
+（正号）   ~（负号）
一元运算符（只有一个元）

练习
#1出租车计价器，3公里以内收费13，超出3公里单价
　2.3元每公里，超过15公里，每公里加收50％
即3.45元/公里
要求：输入公里数，打印出费用金额（以元为单位）
四舍五入

2.输入一个学生的三科成绩：
打印出最高分多少
最低分
平均分
3.给出一个年份，判断是否为闰年并打印结果
4.BMI（Body Mass Index）指数，又称身体质量指数
BMI=体重（公斤）/身高的平方(米)
标准表：BMI<18.5  体重过轻
[18.5,24) 正常
>=24.5超标
输入身高体重，打印BMI值，并打印体重状况
